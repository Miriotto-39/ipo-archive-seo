name: Daily ingest

on:
  schedule:
    - cron: "0 10 * * *"
  workflow_dispatch:

jobs:
  ingest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Phase 1 placeholder
        run: echo "TODO"
